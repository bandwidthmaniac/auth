package lib

const (
	PayloadLengthError = "Make sure `username` & `password` are of length greater than 6"
	UserNotFound       = "User not found"
	InvalidCredentials = "Heads up! The credentials you provided are invalid, try again"
)
